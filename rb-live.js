/* ROBUSTE — rb-live.js
   Every product page references this file. It was missing from the repository,
   so each product page fired a 404 on load (visible error in the console of
   in-app browsers such as Instagram's). This intentional no-op removes the 404
   without changing any behaviour. Put real "live" widgets here later if needed.
*/
;(function () { "use strict"; })();
